# SteelMagnolia — Описание проекта для Claude

## О проекте

**SteelMagnolia** — Progressive Web App (PWA) для отслеживания тренировок. Приложение разработано для пользователя Кристины, UX/UI дизайнера, который любит яркие сочетания и игровую эстетику.

**URL:** https://kiriarcode.github.io/SteelMagnolia/

**Технологии:**
- Alpine.js 3.14+ (реактивность, без build tools)
- Tailwind CSS via CDN (стилизация)
- Lucide Icons (UI иконки)
- Custom SVG иконки для упражнений (icons/exercises.js)
- localStorage (хранение данных)
- Service Worker (офлайн режим, кеширование)

**Архитектура:**
- Один HTML файл (`index.html`) с встроенными шаблонами
- Модульная структура JS: `data.js` → `storage.js` → `exercises.js` → `app.js`
- Без build tools — все загружается через CDN или относительные пути
- PWA с manifest.json и Service Worker

---

## Реализованные фичи

### Основной функционал

**Dashboard (Главный экран):**
- Отображение имени пользователя (Кристина)
- Стрик тренировок с иконкой огня
- Прогресс недели (7 дней с галочками)
- Статистика: кардио минуты, среднее настроение
- Карточка пульсовых зон (макс ЧСС, жиросжигание, интервал)
- Кнопка "Начать тренировку" с подсказкой дня
- Список последних тренировок с эмодзи настроения

**Выбор тренировки:**
- Карточки программ тренировок с градиентами и иконками
- 3 программы: Upper Body, Lower Body, Full Body + Core
- Кнопка "Только кардио"
- Показ количества упражнений и времени кардио
- Кнопка управления упражнениями

**Экран упражнения:**
- Header с прогрессом тренировки (X/Y упражнений)
- Progress bar
- Карточка упражнения с SVG иконкой
- Название и целевые сеты×повторы
- Показ "Прошлый раз: X кг"
- Кнопка "Альтернативы" с выпадающим списком
- Выбор альтернативного упражнения
- Отображение записанных подходов (бейджи)
- Инпут веса с кнопками +/- (шаг 2.5 кг)
- Инпут повторений с кнопками +/-
- Кнопка "Записать подход"
- Автопереход к следующему упражнению после всех сетов
- Нижняя навигация "Назад / Далее"
- Кнопка "Завершить" в header

**Экран кардио:**
- Выбор типа кардио (7 типов: дорожка, эллипсоид, велосипед, гребля, степпер, лыжи, лестница)
- Инпут длительности с +/- (шаг 5 мин)
- Quick-select кнопки: 20, 30, 45, 60 мин
- Поле "Подкаст" (опционально)
- Кнопка "Далее"

**Экран завершения:**
- Слайдер настроения после тренировки (1-10) с эмодзи
- Слайдер общего самочувствия за день (1-10)
- Залитая часть трека слайдеров (градиент)
- Поле заметок (textarea)
- Кнопка "Сохранить тренировку"
- Сохранение в localStorage
- Переход на Dashboard после сохранения

**Таймер отдыха:**
- Автоматический запуск после записи сета
- Обратный отсчёт: 60 сек между подходами, 120 сек между упражнениями
- Экран с обратным отсчётом
- Кнопки "Следующий подход" / "Следующее упражнение"
- Возможность пропустить

**План тренировки:**
- Экран перед стартом тренировки
- Список упражнений с checkbox "Нет в моем зале"
- Фильтрация неактивных упражнений
- Кнопка "Начать тренировку"

### Расширенные функции

**Управление упражнениями:**
- Страница "Управление упражнениями" (вход с экрана выбора тренировки)
- Список тренировок, выбор тренировки
- Добавление упражнения (название, сеты, повторения, иконка, альтернативы, videoUrl, forWhat)
- Редактирование упражнения
- Удаление упражнения
- Drag-and-drop для изменения порядка упражнений
- Сохранение в localStorage (gym_templates)

**Редактирование тренировок:**
- Кнопка редактирования в "Последние тренировки"
- Страница редактирования сохранённой тренировки
- Редактирование подходов (вес, повторения)
- Редактирование кардио, настроения, заметок
- Сохранение изменений через Storage.updateWorkout
- Пересчёт статистики после сохранения

**Удаление тренировок:**
- Кнопка удаления в "Последние тренировки"
- Подтверждение перед удалением

**YouTube видео:**
- Кнопка "Видео" на карточке упражнения
- Модальное окно с iframe YouTube
- URL видео хранится в шаблоне упражнения (videoUrl)
- Поддержка видео для альтернативных упражнений

**Расширенные альтернативы:**
- Альтернативы как объекты: `{ name, forWhat, videoUrl }`
- Поле "Для чего замена" в форме редактирования
- YouTube URL для упражнения и альтернатив

**Хранение данных:**
- Storage.getWorkouts() — получить все тренировки
- Storage.saveWorkout() — сохранить тренировку
- Storage.getStats() / saveStats() — статистика
- Storage.deleteWorkout(id) — удалить тренировку
- Storage.updateWorkout(workout) — обновить тренировку
- Storage.getTemplates() / saveTemplates() — шаблоны тренировок
- Все данные в localStorage

**PWA функционал:**
- manifest.json с иконками (192x192, 512x512)
- Service Worker для кеширования
- Установка на Android/iOS (Add to Home Screen)
- Офлайн режим (базовый)

---

## Стиль приложения (SteelMagnolia)

### Концепция

**Ключевые слова:** Glow • Power • Play • Bloom • Level Up

**Design Pillars:**
- **Glow Aesthetic** — неоновые свечения, градиенты с blur
- **Game Feel** — XP, уровни, badges, progress bars
- **Bold Colors** — яркие контрасты, не бояться цвета
- **Soft Forms** — blob-формы, большие радиусы, плавность

### Цветовая палитра

**Background (Тёмная база):**
- Основной фон: `#0a0a0f` (`bg-[#0a0a0f]`)
- Карточки: `#13131a` (`bg-[#13131a]`)
- Elevated элементы: `#1c1c26` (`bg-[#1c1c26]`)
- Инпуты: `#252532` (`bg-[#252532]`)

**Primary Gradient (Signature):**
- `bg-gradient-to-r from-purple-500 via-pink-500 to-orange-500`
- Purple → Pink → Orange

**Accent Colors:**
- Purple (`#a855f7`) — Primary actions, XP
- Pink (`#ec4899`) — Highlights, hearts
- Orange (`#f97316`) — Streak, fire
- Cyan (`#06b6d4`) — Info, cardio
- Lime (`#84cc16`) — Success, complete
- Yellow (`#facc15`) — Stars, rewards

**Градиенты по типам тренировок:**
- Arms & Upper: `from-purple-500 to-violet-600`
- Glutes & Legs: `from-pink-500 to-rose-600`
- Abs & Core: `from-orange-500 to-amber-600`
- Full Body: `from-pink-500 via-purple-500 to-orange-500`
- Cardio: `from-cyan-500 to-teal-600`

### Glow Effects

Неоновые свечения для кнопок и акцентов:
- `.glow-pink` — розовое свечение
- `.glow-purple` — фиолетовое свечение
- `.glow-orange` — оранжевое свечение

Tailwind: `shadow-[0_0_30px_rgba(236,72,153,0.4)]`

### Типографика

- Font: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif
- Заголовки с градиентом: `bg-gradient-to-r from-purple-400 via-pink-400 to-orange-400 bg-clip-text text-transparent`
- Размеры: text-4xl (Display), text-2xl (Title), text-xl (Heading), text-base (Body), text-sm (Caption)

### Spacing & Layout

- Border Radius: `rounded-2xl` (16px), `rounded-3xl` (24px), `rounded-full`
- Spacing: p-4 (16px), p-5 (20px), p-6 (24px), gap-3 (12px), gap-4 (16px)
- Touch Targets: min-h-[56px] для кнопок

### Анимации

CSS keyframes:
- `glow-pulse` — пульсация свечения
- `float` — плавное движение вверх-вниз
- `sparkle` — мерцание
- `confetti` — конфетти при успехе
- `slide-up` — появление снизу
- `pop` — масштабирование при появлении

Transitions:
- `transition-all duration-200`
- `active:scale-[0.98]` на кнопках
- `hover:shadow-[0_0_30px_rgba(236,72,153,0.4)]`

### Компоненты

**Primary Button (CTA):**
- Градиент `from-purple-500 via-pink-500 to-orange-500`
- Glow shadow
- `rounded-2xl`, `active:scale-[0.98]`

**Workout Card:**
- Градиентный фон с прозрачностью (`from-pink-500/20 to-rose-600/20`)
- Border glow (`border-pink-500/30`)
- `rounded-3xl`, `backdrop-blur-sm`
- `hover:shadow-[0_0_30px_rgba(236,72,153,0.2)]`

**Input с +/-:**
- Кнопки: `bg-[#252532]` с border `border-pink-500/20`
- Значение: градиентный текст `from-purple-400 to-pink-400`
- `rounded-2xl`, `active:scale-95`

**Badges (Recorded Sets):**
- `bg-lime-500/20`, `border-lime-500/30`
- `rounded-xl`

**Bottom Navigation:**
- `bg-[#13131a]/90` с `backdrop-blur-lg`
- Активная кнопка: градиент с glow
- Неактивная: `bg-[#252532]`

---

## Структура данных

### Программы тренировок

Файл: `js/data.js`

```javascript
const WORKOUT_TEMPLATES = {
  tuesday: {
    name: 'Upper Body',
    emoji: '💪',
    gradient: 'bg-gradient-to-r from-purple-500 to-violet-600',
    cardio: 25,
    exercises: [
      {
        id: 'lat_pulldown',
        name: 'Тяга верхнего блока',
        sets: 3,
        reps: 12,
        icon: 'lat_pulldown',
        lastWeight: 40,
        videoUrl: '', // Опционально
        alts: [
          { name: 'Альтернатива', forWhat: 'Для чего', videoUrl: '' }
        ]
      }
    ]
  }
}
```

### Типы кардио

Файл: `js/app.js` (внутри `gymTracker()`)

```javascript
cardioTypes: [
  { id: 'treadmill', name: 'Дорожка', icon: 'treadmill' },
  { id: 'elliptical', name: 'Эллипсоид', icon: 'elliptical' },
  { id: 'bike', name: 'Велосипед', icon: 'bike' },
  { id: 'rower', name: 'Гребля', icon: 'rower' },
  { id: 'stepper', name: 'Степпер', icon: 'stepper' },
  { id: 'ski', name: 'Лыжи', icon: 'ski' },
  { id: 'stairmaster', name: 'Лестница', icon: 'stairmaster' },
]
```

---

## Текущее состояние

**Пользователь:** Кристина  
**Программ тренировок:** 3 (tuesday, thursday, saturday)  
**Всего упражнений:** 23  
**Типов кардио:** 7  
**Доступных иконок:** 24+ (включая кардио)

**Основные страницы:**
- dashboard
- select-workout
- workout-plan
- workout
- rest
- cardio
- complete
- edit-workout
- manage-exercises

---

## Важные ограничения

1. **No build tools** — нет npm, webpack, vite
2. **Script order** — data.js → storage.js → exercises.js → app.js
3. **Page switching** — использовать `x-if`, не `x-show`
4. **Paths** — всегда `./` относительные пути для GitHub Pages
5. **UI language** — русский

---

## Связанные документы

- `docs/FEATURES.md` — полный список фич и roadmap
- `docs/STYLE_GUIDE.md` — детальное руководство по стилям
- `docs/WORKOUT_PROGRAM_FORMAT.md` — формат данных для упражнений
- `docs/DATA_FORMAT.md` — форматы данных для добавления контента
- `DATA_FLOW.md` — поток данных в приложении
- `.cursorrules` — правила для Cursor
