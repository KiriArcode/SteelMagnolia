# Инструкция для Cursor: Формат программ тренировок

## Задача
Описание формата данных для программ тренировок и упражнений в PWA приложении GymBro.

## Структура данных

### Формат программы тренировок

Файл: `js/data.js`

```javascript
const WORKOUT_TEMPLATES = {
  tuesday: {
    name: 'Upper Body',
    emoji: '💪',
    gradient: 'bg-gradient-to-r from-purple-500 to-violet-600',
    cardio: 30,
    exercises: [
      { 
        id: 'lat_pulldown', 
        name: 'Тяга верхнего блока', 
        sets: 4, 
        reps: 12, 
        icon: 'lat_pulldown', 
        lastWeight: 60,
        videoUrl: '',  // Опционально: URL YouTube видео
        alts: [  // Альтернативы как объекты
          { 
            name: 'Тяга гантели в наклоне', 
            forWhat: 'Если нет блока', 
            videoUrl: '' 
          },
          { 
            name: 'Подтягивания в гравитроне', 
            forWhat: '', 
            videoUrl: 'https://youtube.com/...' 
          }
        ]
      },
      // ... остальные упражнения
    ]
  },
  thursday: {
    name: 'Lower Body',
    emoji: '🦵',
    gradient: 'bg-gradient-to-r from-pink-500 to-rose-600',
    cardio: 20,
    exercises: [
      // упражнения для ног
    ]
  },
  saturday: {
    name: 'Full Body + Core',
    emoji: '🔥',
    gradient: 'bg-gradient-to-r from-pink-500 via-purple-500 to-orange-500',
    cardio: 30,
    exercises: [
      // упражнения для всего тела
    ]
  }
};
```

### Поля программы тренировки

| Поле | Тип | Обязательное | Описание |
|------|-----|--------------|----------|
| `name` | `string` | ✅ Да | Название программы тренировки |
| `emoji` | `string` | ✅ Да | Эмодзи для визуализации (1 символ) |
| `gradient` | `string` | ✅ Да | Tailwind CSS класс градиента |
| `cardio` | `number` | ✅ Да | Планируемое время кардио в минутах |
| `exercises` | `array` | ✅ Да | Массив упражнений |

### Поля упражнения

| Поле | Тип | Обязательное | Описание |
|------|-----|--------------|----------|
| `id` | `string` | ✅ Да | Уникальный ID (snake_case) |
| `name` | `string` | ✅ Да | Название упражнения на русском |
| `sets` | `number` | ✅ Да | Количество подходов |
| `reps` | `number\|string` | ✅ Да | Повторения (число) или время (строка) |
| `icon` | `string` | ✅ Да | Имя иконки из `icons/exercises.js` |
| `lastWeight` | `number` | ✅ Да | Последний вес (кг). Может быть отрицательным для гравитрона |
| `videoUrl` | `string` | ❌ Нет | URL YouTube видео упражнения (опционально) |
| `alts` | `array` | ❌ Нет | Альтернативные упражнения (массив объектов) |
| `description` | `string` | ❌ Нет | Дополнительные инструкции (опционально, для будущего использования) |

### Формат альтернативных упражнений

Альтернативы должны быть объектами с полями:

```javascript
alts: [
  { 
    name: 'Альтернатива 1',           // Название альтернативы
    forWhat: 'Если нет оборудования',  // Для чего замена (опционально)
    videoUrl: ''                       // URL YouTube видео (опционально)
  },
  { 
    name: 'Альтернатива 2', 
    forWhat: '', 
    videoUrl: 'https://youtube.com/watch?v=...' 
  }
]
```

**Примечание:** Для обратной совместимости система поддерживает старый формат (массив строк), который автоматически конвертируется в объекты.

### Доступные градиенты по типам тренировок

Согласно Style Guide SteelMagnolia:

| Тип тренировки | Градиент Tailwind | Пример |
|----------------|-------------------|--------|
| **Arms & Upper** | `from-purple-500 to-violet-600` | Upper Body |
| **Glutes & Legs** | `from-pink-500 to-rose-600` | Lower Body |
| **Abs & Core** | `from-orange-500 to-amber-600` | Core тренировка |
| **Full Body** | `from-pink-500 via-purple-500 to-orange-500` | Full Body + Core |
| **Cardio** | `from-cyan-500 to-teal-600` | Только кардио |

---

## Формат типов кардио

Файл: `js/app.js` (внутри функции `gymTracker()`)

```javascript
cardioTypes: [
  { id: 'treadmill', name: 'Дорожка', icon: 'treadmill' },
  { id: 'elliptical', name: 'Эллипсоид', icon: 'elliptical' },
  { id: 'bike', name: 'Велосипед', icon: 'bike' },
  { id: 'rower', name: 'Гребля', icon: 'rower' },
  { id: 'stepper', name: 'Степпер', icon: 'stepper' },
  { id: 'ski', name: 'Лыжи', icon: 'ski' },
  { id: 'stairmaster', name: 'Лестница', icon: 'stairmaster' },
]
```

### Поля типа кардио

| Поле | Тип | Обязательное | Описание |
|------|-----|--------------|----------|
| `id` | `string` | ✅ Да | Уникальный ID типа кардио |
| `name` | `string` | ✅ Да | Название на русском |
| `icon` | `string` | ✅ Да | Имя иконки из `icons/exercises.js` |

---

## Примеры

### Пример 1: Полная программа тренировки

```javascript
const WORKOUT_TEMPLATES = {
  tuesday: {
    name: 'Upper Body',
    emoji: '💪',
    gradient: 'bg-gradient-to-r from-purple-500 to-violet-600',
    cardio: 30,
    exercises: [
      {
        id: 'lat_pulldown',
        name: 'Тяга верхнего блока',
        sets: 4,
        reps: 12,
        icon: 'lat_pulldown',
        lastWeight: 60,
        videoUrl: 'https://youtube.com/watch?v=example',
        alts: [
          { 
            name: 'Тяга гантели в наклоне', 
            forWhat: 'Если нет блока', 
            videoUrl: '' 
          },
          { 
            name: 'Подтягивания в гравитроне', 
            forWhat: '', 
            videoUrl: '' 
          }
        ]
      }
    ]
  }
};
```

### Пример 2: Упражнение с временем вместо повторений

```javascript
{
  id: 'plank_hold',
  name: 'Планка',
  sets: 3,
  reps: '45-60 сек',           // Строка для времени
  icon: 'plank',
  lastWeight: 0,                // Без веса
  videoUrl: '',
  alts: [
    { name: 'Планка на локтях', forWhat: '', videoUrl: '' },
    { name: 'Планка динамичная', forWhat: '', videoUrl: '' }
  ]
}
```

### Пример 3: Упражнение с отрицательным весом (гравитрон)

```javascript
{
  id: 'assisted_pullup',
  name: 'Подтягивания в гравитроне',
  sets: 3,
  reps: 10,
  icon: 'pull_up',
  lastWeight: -30,              // Отрицательное = помощь гравитрона
  videoUrl: '',
  alts: [
    { name: 'Тяга верхнего блока', forWhat: '', videoUrl: '' },
    { name: 'Австралийские подтягивания', forWhat: '', videoUrl: '' }
  ]
}
```

---

## Доступные иконки

Иконки определены в `icons/exercises.js`. Доступные иконки:

### Силовые упражнения
- `dumbbell` (по умолчанию)
- `lat_pulldown`, `shoulder_press`, `cable_row`, `dumbbell_press`
- `bicep_curl`, `lateral_raise`
- `leg_press`, `romanian_deadlift`, `glute_bridge`
- `leg_extension`, `leg_curl`, `pull_up`, `squat`, `hyperextension`

### Упражнения на кор
- `plank`, `side_plank`

### Кардио
- `treadmill`, `elliptical`, `bike`, `rower`, `stepper`, `ski`, `stairmaster`

**Примечание:** Если иконка не найдена, используется `dumbbell` по умолчанию.

---

## Технические требования

1. **Файл `js/data.js`** содержит объект `WORKOUT_TEMPLATES` (не ES6 export)
2. **Файл `js/app.js`** содержит массив `cardioTypes` внутри функции `gymTracker()`
3. **Файл `icons/exercises.js`** содержит объект `EXERCISE_ICONS` с SVG иконками
4. Все данные сохраняются в `localStorage` через модуль `Storage` (`js/storage.js`)

---

## Структура ответа от Cursor

После выполнения верни:
1. ✅ Обновленные файлы (data.js, app.js, exercises.js)
2. 📊 Статистика:
   - Всего дней: 3 (tuesday, thursday, saturday)
   - Типов кардио: 7
   - Доступных иконок: ~20+
3. 🎨 Список необходимых иконок (если нужно добавить новые)
4. 💡 Предложения по UI/UX

---

## Связанные документы

- `docs/DATA_FORMAT.md` — Форматы данных для добавления контента
- `docs/STYLE_GUIDE.md` — Руководство по стилям и градиентам
- `docs/FEATURES.md` — Список функций приложения
